<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/css/styles.css" />

    <script src="/js/chart.js"></script>
  </head>
  <body>
    <header class="bg-white shadow">
      <nav class="container mx-auto px-4 py-4">
        <ul class="flex space-x-4 items-center justify-end">
          <li><a href="/" class="text-blue-600 hover:underline">Home</a></li>

          <% if (user) { %>
          <li>
            <a href="/mypolls" class="text-blue-600 hover:underline"
              >My Polls</a
            >
          </li>
          <li>
            <a href="/new_poll" class="text-blue-600 hover:underline"
              >Create Poll</a
            >
          </li>
          <li>
            <form action="/auth/logout" method="POST">
              <button
                type="submit"
                class="text-blue-600 hover:underline bg-transparent border-none cursor-pointer"
              >
                Logout
              </button>
            </form>
          </li>
          <% } else { %>
          <li>
            <a href="/login" class="text-blue-600 hover:underline">Login</a>
          </li>
          <li>
            <a href="/register" class="text-blue-600 hover:underline"
              >Register</a
            >
          </li>
          <% } %>
        </ul>
      </nav>
    </header>

    <% if (message) { %>
    <div
      class="max-w-2xl mx-auto mt-4 p-4 rounded-lg text-white <%= message.type === 'success' ? 'bg-green-500' : 'bg-red-500' %>"
    >
      <%= message.text %>
    </div>
    <% } %>

    <main><%- body %></main>
  </body>
</html>
